---
# ---------- CLEAN OLD CONFIG ----------
- name: Remove old repo/key files
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/apt/sources.list.d/jenkins.list
    - /etc/apt/sources.list.d/nodesource.list
    - /etc/apt/sources.list.d/trivy.list
    - /etc/apt/keyrings/jenkins-keyring.asc
    - /etc/apt/keyrings/nodesource.gpg
    - /usr/share/keyrings/trivy.gpg
    - /tmp/nodesource-repo.gpg.key

- name: Remove legacy Jenkins repo entries
  lineinfile:
    path: /etc/apt/sources.list
    regexp: '^deb .*pkg\.jenkins\.io/.*$'
    state: absent
  ignore_errors: true

- name: Remove legacy NodeSource repo entries
  lineinfile:
    path: /etc/apt/sources.list
    regexp: '^deb .*deb\.nodesource\.com/.*$'
    state: absent
  ignore_errors: true

# ---------- JENKINS ----------
- name: Add Jenkins GPG key
  get_url:
    url: https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
    dest: /etc/apt/keyrings/jenkins-keyring.asc
    mode: '0644'
    force: yes

- name: Add Jenkins repository
  copy:
    dest: /etc/apt/sources.list.d/jenkins.list
    mode: '0644'
    content: |
      deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian-stable binary/

# ---------- NODE.JS ----------
- name: Download NodeSource GPG key
  get_url:
    url: https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key
    dest: /tmp/nodesource-repo.gpg.key
    mode: '0644'
    force: yes

- name: Convert NodeSource key
  command: gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg /tmp/nodesource-repo.gpg.key
  args:
    creates: /etc/apt/keyrings/nodesource.gpg

- name: Add NodeSource repository
  copy:
    dest: /etc/apt/sources.list.d/nodesource.list
    mode: '0644'
    content: |
      deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_20.x nodistro main

# ---------- TRIVY ----------
- name: Add Trivy key
  shell: |
    wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor -o /usr/share/keyrings/trivy.gpg
  args:
    creates: /usr/share/keyrings/trivy.gpg

- name: Add Trivy repo
  copy:
    dest: /etc/apt/sources.list.d/trivy.list
    mode: '0644'
    content: |
      deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main

# ---------- INSTALL ----------
- name: Update apt cache
  command: apt-get update
  changed_when: false

- name: Install Jenkins, Node.js, Trivy and openjdk
  apt:
    name:
      - jenkins
      - nodejs
      - trivy
      - fontconfig     
      - openjdk-17-jre
    state: present

# ---------- SERVICE ----------
- name: Enable and start Jenkins
  service:
    name: jenkins
    state: started
    enabled: yes

- name: Restart Jenkins
  service:
    name: jenkins
    state: restarted

- name: Install ansible
  apt:
    name: ansible
    state: present
    update_cache: true